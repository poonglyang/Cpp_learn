#include <iostream>

int GlovalValue = 1000;

static int staticGValue = 20;	// static가 붙으면 변수가 선언된 공간에서만 사용할 수 있다

//class CountClass;

int main() {
	//std::cout << CountClass::GetCount() << std::endl;
}